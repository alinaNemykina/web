export default {
  actions: {
    async login({dispatch, commit}, {email, password}) {
      try {
        // await signInWithEmailAndPassword(email, password)
      } catch (e) {
      }
    }
  }
}
